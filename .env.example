# このファイルはコミット対象です。実際の値は書かず、変数名とプレースホルダのみを記載します。
# 開発時は本ファイルを .env にコピーし、実際のシークレットを設定してください（.env はコミット対象外です）。

# アプリケーション層（Rails）の config/master.key に相当するシークレットです。
# config/master.key 自体はコミットしないため、docker compose 起動時はこの環境変数で渡してください。
RAILS_MASTER_KEY=
